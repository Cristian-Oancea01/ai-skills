# examples.md

## Good
```js
function sum(a, b) {
  return a + b;
}
```

## Avoid
```js
// adds two numbers together and returns result
function sum(a, b) {
  const result = a + b;
  return result;
}
```

## Rule of thumb
- Smallest correct change.
- No extra code, no extra comments.
